// Swift example - Powerful and intuitive!

print("🍎 Hello from Swift!")
print("Swift: Safe, fast, and expressive")

// Variables
let message = "Welcome to AI-IDE"
print(message)

// Function
func add(_ a: Int, _ b: Int) -> Int {
    return a + b
}

let sum = add(5, 10)
print("5 + 10 = \(sum)")

// Array example
let numbers = [1, 2, 3, 4, 5]
let total = numbers.reduce(0, +)
print("Sum of \(numbers) = \(total)")

// Loop example
for num in numbers {
    print("Number: \(num)")
}

// Struct example
struct Person {
    var name: String
    var version: Int
}

let person = Person(name: "AI-IDE", version: 1)
print("\nPerson: \(person.name) v\(person.version)")
